# 读完有话说 4.1 · 粉红编辑室

这是可独立托管的静态网站发布包。打开 `index.html` 即可运行；资料默认只在当前浏览器读取，用户自带模型的 Key 只保留在当前会话。

- `index.html`：首屏与无脚本回退内容
- `app.js`：导入、拆解、练习、写作与导出
- `styles.css`：4.1 视觉样式；不依赖外部字体 CDN
- `assets/`：本地图片资源

免费体验服务和用户自带模型均为按需网络请求；网络不可用时，本地提炼仍可使用。
